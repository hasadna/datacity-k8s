{{-
  include "cco.exec" (
  dict
    "name" "solr-reindex"
    "set_instance_id" "true"
    "parameters" (list
      (dict "name" "INSTANCE_NAME")
      (dict "name" "ARGS" "default" "rebuild")
    )
    "exec_script" `exec $instance_id_ckan_exec ckan-paster --plugin=ckan search-index -c /etc/ckan/production.ini $ARGS`
  )
}}
